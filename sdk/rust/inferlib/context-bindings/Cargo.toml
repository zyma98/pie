[package]
name = "inferlib-context-bindings"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = { workspace = true }

# Import dummy bindings instead of regenerating
inferlib-dummy-bindings = { path = "../dummy-bindings" }
