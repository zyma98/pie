[package]
name = "output-validation"
version = "0.1.0"
description = "Output validation by computing normalized probabilities over candidate strings (Python)"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"
python-runtime = "^0.2.0"

[parameters]

[dependencies]
inferlib-inference = "0.1.0"
