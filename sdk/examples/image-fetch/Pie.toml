[package]
name = "image-fetch"
version = "0.1.0"
description = "Fetching and decoding images over HTTP"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

[parameters]
url = {type = "string", optional = true, description = "The URL of the image to fetch (default: 'https://www.ilankelman.org/stopsigns/australia.jpg')"}
