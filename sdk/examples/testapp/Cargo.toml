[package]
name = "testapp"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
testlib-bindings = { path = "../../rust/testlib/testlib-bindings" }
inferlib-run-bindings = { path = "../../rust/inferlib/run-bindings" }
