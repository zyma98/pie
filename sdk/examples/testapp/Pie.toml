[package]
name = "testapp"
version = "0.1.0"
description = "Minimal test app to reproduce borrow<model> resource passing bug"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

[dependencies]
testlib = "0.1.0"
