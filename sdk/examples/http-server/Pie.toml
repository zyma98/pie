[package]
name = "http-server"
version = "0.1.0"
description = "HTTP server inferlet demonstrating the wasi:http/incoming-handler interface"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

# No CLI parameters - this is an HTTP server that receives input via HTTP requests
[parameters]
