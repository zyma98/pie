[package]
name = "parallel-generation-old"
version = "0.1.0"
description = "Parallel text generation from forked contexts with shared KV cache"
authors = ["Pie Team"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

[parameters]
max_tokens = {type = "int", optional = true, description = "Max tokens to generate for each prompt (default: 128)"}

[dependencies]
inferlib-inference-old = "0.1.0"
