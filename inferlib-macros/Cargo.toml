[package]
name = "inferlib-macros"
version = "0.1.0"
edition = "2021"
description = "Procedural macros for the inferlib library"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
