package inferlib:environment;

world environment-provider {
    // Import from the host runtime
    import inferlet:core/runtime;
    
    // Export our clean environment interface
    export runtime;
}
