package pie:inferlet;

world traits {
    import allocate;
    import forward;
    import input-text;
    import input-image;
    import tokenize;
    import output-text;
}

world imports {
    include traits;
    import core;
}

world inferlet {
    include imports;
    export run;
}

world inferlet-server {
    include imports;
    export wasi:http/incoming-handler@0.2.4;
}